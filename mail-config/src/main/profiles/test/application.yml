server:
  port: 9000

spring:
  redis:
        host: 127.0.0.1
        port: 8888
        password: qazwsx@12345
        pool:
          max-idle: 8
          min-idle: 0
          max-active: 8
          max-wait: -1
  application:
    name: mail-server